<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>example</groupId>
  <artifactId>gatling-ecomm-maven-java</artifactId>
  <version>1.0.0</version>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- версии Gatling -->
    <gatling.version>3.15.0</gatling.version>
    <gatling-maven-plugin.version>4.21.0</gatling-maven-plugin.version>
  </properties>

  <dependencies>
    <!-- Основные Gatling charts -->
    <dependency>
      <groupId>io.gatling.highcharts</groupId>
      <artifactId>gatling-charts-highcharts</artifactId>
      <version>${gatling.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Gatling Maven plugin -->
      <plugin>
        <groupId>io.gatling</groupId>
        <artifactId>gatling-maven-plugin</artifactId>
        <version>${gatling-maven-plugin.version}</version>
        <configuration>
          <jvmArgs>
            <jvmArg>-DTEST_TYPE=${TEST_TYPE}</jvmArg>
            <jvmArg>-DDURATION=${DURATION}</jvmArg>
            <jvmArg>-DDOMAIN=${DOMAIN}</jvmArg>
          </jvmArgs>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <!-- 🔥 ПРОФИЛИ НАГРУЗКИ -->
  <profiles>
    <profile>
      <id>instant</id>
      <properties>
        <TEST_TYPE>INSTANT_USERS</TEST_TYPE>
        <DURATION>60</DURATION>
        <DOMAIN>acetoys.uk</DOMAIN>
      </properties>
    </profile>

    <profile>
      <id>ramp</id>
      <properties>
        <TEST_TYPE>RAMP_USERS</TEST_TYPE>
        <DURATION>120</DURATION>
        <DOMAIN>acetoys.uk</DOMAIN>
      </properties>
    </profile>

    <profile>
      <id>complex</id>
      <properties>
        <TEST_TYPE>COMPLEX_INJECTION</TEST_TYPE>
        <DURATION>180</DURATION>
        <DOMAIN>acetoys.uk</DOMAIN>
      </properties>
    </profile>

    <profile>
      <id>closed</id>
      <properties>
        <TEST_TYPE>CLOSED_MODEL</TEST_TYPE>
        <DURATION>120</DURATION>
        <DOMAIN>acetoys.uk</DOMAIN>
      </properties>
    </profile>
  </profiles>

</project>